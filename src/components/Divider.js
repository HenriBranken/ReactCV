import React from "react";
import classes from "./Divider.module.css";

const Divider = () => {
  {/* A divider is simply a straight line that demarcates content. */}
  return <div className={classes.Divider}></div>;
};

export default Divider;
